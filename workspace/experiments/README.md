# Experiments

Put self-contained test code here. Name each experiment by purpose, keep its
inputs small, and move proven production code into the main project rather than
leaving it here indefinitely.
