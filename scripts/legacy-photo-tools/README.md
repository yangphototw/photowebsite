# Legacy photo tools

These scripts came from the earlier Gemini CLI workflow. Several contain
hard-coded paths or rely on the retired `Albums` workflow, so do not run them
for normal website updates. They are retained only as historical reference
while the Website Queue workflow replaces them.
