# Scripts

`bootstrap_website_queue.py` creates the Website Queue folder structure and
seeds it with copies of the images currently used by the site.

`legacy-photo-tools/` contains earlier one-off Gemini CLI scripts. They are
preserved for reference but are not part of the current publishing workflow.
